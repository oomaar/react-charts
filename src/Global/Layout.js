import React from 'react';

export const Layout = ({ children }) => {
    return (
        <div>
            {/* Header */}
            {/* Sidebar */}
            {/* Footer */}
        </div>
    );
};
